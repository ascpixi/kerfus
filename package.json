{
  "name": "kerfus",
  "version": "1.0.0",
  "description": "ascpixi's personal bot for Hack Club's Slack",
  "private": true,
  "main": "dist/app.js",
  "scripts": {
    "build": "node fix-atproto-lexicon.js && tsc",
    "start": "npm run build && node ./dist/app.js",
    "lint": "npx @biomejs/biome check src",
    "lint:fix": "npx @biomejs/biome check --write src",
    "test": "npm run build && npm run lint"
  },
  "author": "ascpixi",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ascpixi/kerfus.git"
  },
  "bugs": {
    "url": "https://github.com/ascpixi/kerfus/issues"
  },
  "dependencies": {
    "@atproto/api": "^0.16.5",
    "@slack/bolt": "^4.4.0",
    "dotenv": "~17.2.1",
    "lowdb": "^7.0.1",
    "steno": "^4.0.2"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.3",
    "typescript": "^5.9.2"
  }
}
